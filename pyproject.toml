[project]
name = "fsdantic"
version = "0.7.0"
description = "Type-safe Pydantic models and query interface for AgentFS SDK"
readme = "README.md"
requires-python = ">=3.13"
license = { text = "MIT" }

authors = [
  { name = "Bullish Design", email = "BullishDesignEngineering@gmail.com" },
]

dependencies = [
    "agentfs-sdk @ git+https://github.com/Bullish-Design/agentfs@v0.6.4-pyturso-0.7.2#subdirectory=sdk/python",
    "pydantic>=2.0.0",
    "pyturso>=0.7.2,<0.8",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "hypothesis>=6.0.0",
    "ruff>=0.1.0",
]

#[project.urls]
#homepage = "https://github.com/Bullish-Design/template-py"
#repository = "https://github.com/Bullish-Design/template-py.git"

[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
# Tell hatch which package to ship (your code lives in src/template_py)
packages = ["src/fsdantic"]

[tool.hatch.metadata]
# fsdantic depends on the agentfs-sdk fork via a direct git reference
# (agentfs-sdk @ git+https://...); hatchling rejects direct references in
# project.dependencies unless this is enabled.
allow-direct-references = true

[tool.hatch.build.targets.sdist]
include = [
  "src",
  "README.md",
#  "SPEC.md",
#  "docs",
  "tests",
  "pyproject.toml",
]

[tool.uv]
prerelease = "allow"

[tool.ruff]
line-length = 120
target-version = "py313"
src = ["src"]

[tool.ruff.lint]
select = [
  "E",
  "F",
  "I",
  "UP",
  "B",
]
ignore = []

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"


